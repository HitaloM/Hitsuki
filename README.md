# SophieBot

## Requirements
+ Installed docker
+ brain

## Running
    docker pull mryacha:willuploadlater
    docker run -v bot_conf.json:/sophie_bot/bot_conf.json bot

## Build own
    docker build . -t bot
